view "section" class="p4"
  article
    .h1 Works

    .works
      each project in controller.model
        a.work.lib{{bind-attr href="project.link"}}
          h3 = project.name
          div = project.description

